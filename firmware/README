How to compile the firmware:
---------------------------------

1. Launch the Arduino IDE(arduino-0022) and open up the firmware.pde

2. Locate the "config.h" tab and replace settings with your own (YOUR CALLSIGN
   GOES THERE).

3. When uploading the firmware, unplug the GPS or flashing will fail with an
error.
